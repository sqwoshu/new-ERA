    def click(self):
        browser = self.browser
        index_click = self.index_click

        time.sleep(1)
        c = self.browser.find_elements(By.CSS_SELECTOR, ".panel-group.task_item")
        for i in c:
            d = i.get_attribute('id').split('task-item-')[1]
            if d not in index_click:
                self.index_click.append(d)

        browser.find_element(By.XPATH,f'//*[@id="task-item-{index_click[0]}"]/div/div/div/div[4]/a').click()
        del index_click[0]

    def sleep(self):
        browser = self.browser

        ballans_ok = browser.find_element(By.ID, 'user_points_balance').text
        print('ok', ballans_ok)

        print("go to bed")
        time.sleep(60)
